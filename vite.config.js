import { defineConfig } from 'vite';

export default defineConfig({
  // GitHub Pages project site → served at inthuja-nava.github.io/hello-web/
  // base must match the real repo name exactly.
  base: '/hello-web/',
});
